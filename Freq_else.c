#include<stdio.h>
int main ()
  {
  int a[10],i,freq=0,key;
  for(i=0;i<10;i++)
    {
    printf("Enter number");
    scanf("%d",a[i]);
    }
    printf("Enter no to find freq");
    scanf(%d",&key);
          for(i=0;i<10;i++)
          ig(a[i]==key)
          freq++;
          printf("Freq of %d is %d ",key,freq);
          return 0;
          }
